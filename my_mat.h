#define size 10
void fillMatrixFromInput(int[size][size]);
void copyMatrix(int[size][size] , int[size][size]);
void takeShortestPath(int[size][size]);
int min(int, int);
int isTherePath(int[size][size]);
int shortestPath(int[size][size]);